@baseUrl = {{$dotenv API_BASE_URL}}
@token = {{$dotenv ACCESS_TOKEN}}

### Get all lists (grouped)
GET {{baseUrl}}/lists
Authorization: Bearer {{token}}

### List FAVORITES
GET {{baseUrl}}/lists/FAVORITES?limit=5
Authorization: Bearer {{token}}

### Add to WISHLIST (replace with a real bookId)
POST {{baseUrl}}/lists/WISHLIST
Content-Type: application/json
Authorization: Bearer {{token}}

{
  "bookId": "<REPLACE_BOOK_ID>"
}

### Remove from WISHLIST (replace with a real bookId)
DELETE {{baseUrl}}/lists/WISHLIST/<REPLACE_BOOK_ID>
Authorization: Bearer {{token}}
