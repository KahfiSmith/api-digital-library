@baseUrl = {{$dotenv API_BASE_URL}}
@token = {{$dotenv ACCESS_TOKEN}}
@token = {{token}}

### List books
GET {{baseUrl}}/books?limit=5

### Get a book by ID (replace with a real ID from list)
GET {{baseUrl}}/books/{{bookId}}

### Create a book (requires ADMIN/LIBRARIAN; replace categoryId)
POST {{baseUrl}}/books
Content-Type: application/json
Authorization: Bearer {{token}}

{
  "title": "My Test Book",
  "authors": ["Test Author"],
  "categoryId": "<REPLACE_WITH_CATEGORY_ID>",
  "language": "en",
  "tags": ["Test"]
}

### Update a book (replace :id)
PUT {{baseUrl}}/books/{{bookId}}
Content-Type: application/json
Authorization: Bearer {{token}}

{
  "title": "My Test Book Updated"
}

### Delete a book (replace :id)
DELETE {{baseUrl}}/books/{{bookId}}
Authorization: Bearer {{token}}
