@baseUrl = {{$dotenv API_BASE_URL}}
@token = {{$dotenv ACCESS_TOKEN}}
@token = {{token}}

### Logout using cookie (automatic)
POST {{baseUrl}}/auth/logout

> {%
// Clear globals after logout
client.global.set("token", "");
client.global.set("refreshToken", "");
client.log("🧹 Cleared global token & refreshToken");
%}

### Logout using body
POST {{baseUrl}}/auth/logout
Content-Type: application/json

{
  "refreshToken": "your_refresh_token_here"
}

### Logout with invalid token (should still succeed - best effort)
POST {{baseUrl}}/auth/logout
Content-Type: application/json

{
  "refreshToken": "invalid_token"
}
