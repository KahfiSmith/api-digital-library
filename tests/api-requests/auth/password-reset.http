@baseUrl = {{$dotenv API_BASE_URL}}
@email = {{$dotenv TEST_EMAIL}}

### Request password reset
POST {{baseUrl}}/auth/request-password-reset
Content-Type: application/json

{
  "email": "{{email}}"
}

### Request reset for non-existent email (should still return success)
POST {{baseUrl}}/auth/request-password-reset
Content-Type: application/json

{
  "email": "nonexistent@example.com"
}

### Reset password with token
POST {{baseUrl}}/auth/reset-password
Content-Type: application/json

{
  "tokenId": "your_reset_token_id_here",
  "token": "your_reset_token_here",
  "newPassword": "NewPassword123!"
}

### Reset with invalid token (should fail with 400)
POST {{baseUrl}}/auth/reset-password
Content-Type: application/json

{
  "tokenId": "invalid_id",
  "token": "invalid_token",
  "newPassword": "NewPassword123!"
}

### Reset with missing fields (should fail with 400)
POST {{baseUrl}}/auth/reset-password
Content-Type: application/json

{
  "tokenId": "some_id",
  "token": "some_token"
}
