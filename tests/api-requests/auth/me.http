@baseUrl = {{$dotenv API_BASE_URL}}
@token = {{$dotenv ACCESS_TOKEN}}
@token = {{token}}

### Get current user (uses auth cookie automatically or header)
GET {{baseUrl}}/auth/me

### Get current user with Authorization header
GET {{baseUrl}}/auth/me
Authorization: Bearer {{token}}

### Get current user without authentication (should fail with 401)
GET {{baseUrl}}/auth/me
